@model ExpenseLimit

@{
    ViewBag.Title = "Expense Limit";
}

<form method="post">
    <div class="row justify-content-center m-3">
        <div class="col-sm-8">
            <div class="card">
                <div class="card-header text-center bg-primary">
                    <h2><b>Edit Expense Limit Id: @Model.Id</b></h2>
                    @Html.HiddenFor(model => model.Id)
                </div>

                <div class="card-body text-left">
                    <div class="form-group row">
                        <label asp-for="ExpenseCategory" class="col-sm-2 col-form-label">Category</label>
                        <div class="col-sm-10">
                            <select class="form-control" asp-for="ExpenseCategory"
                                    asp-items="Html.GetEnumSelectList<CategoryEnum>()">
                                <option value="">Please select a Category</option>
                            </select>
                            <span asp-validation-for="ExpenseCategory" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-group row">
                        <label asp-for="ExpenseType" class="col-sm-2 col-form-label">Type</label>
                        <div class="col-sm-10">
                            <select class="form-control" asp-for="ExpenseType"
                                    asp-items="Html.GetEnumSelectList<TypeEnum>()">
                                <option value="">Please select a Type</option>
                            </select>
                            <span asp-validation-for="ExpenseType" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-group row">
                        <label asp-for="Limit" class="col-sm-2 col-form-label">Monthy Limit £</label>
                        <div class="col-sm-10">
                            <input class="form-control" asp-for="Limit" type="number" min="0" step="0.01" />
                            <span asp-validation-for="Limit" class="text-danger"></span>
                        </div>
                    </div>
                </div>

                <div class="card-footer text-center">
                    <a asp-controller="ExpenseLimit" asp-action="Index" class="btn btn-primary">Back</a>
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>
            </div>
        </div>
    </div>
</form>
